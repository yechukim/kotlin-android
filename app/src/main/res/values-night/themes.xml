<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BBomoTimer" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/tomato</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/tomato</item>
    </style>
</resources>